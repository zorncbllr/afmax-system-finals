services:
  # nginx
  web:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf

  # php
  server:
    build:
      dockerfile: ./server/Dockerfile
    volumes:
      - ./server:/server

  # mysql
  # redis
